server:
  port: 8082

spring:
  application:
    name: TRADE-SERVICE

eureka:
  instance:
    ip-address: 127.0.0.1
    prefer-ip-address: true

  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8081

spring:
  application:
    name: USER-SERVICE

eureka:
  instance:
    ip-address: 127.0.0.1
    prefer-ip-address: true

  client:
    service-url:
      defaultZone: http://localhost:8761/eureka